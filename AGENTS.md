# 财会生物鉴定中心：跨设备接手

项目进度直接从 GitHub 恢复，不依赖本机专属 Skill、旧对话或 Obsidian，不另建项目状态副本。当前用户要求与有效授权优先，本文不增加自动化、合并或发布权限。

## 开始前

- 核对仓库身份，先运行 `git status --short --branch`，再 `git fetch origin`；比较本机、上游和任务基线，检查未提交、未推送修改及本机在途任务，不盲目 pull 或切换。
- 先读 [README](README.md)，再按任务读取 [素材来源](ART-CREDITS.md)、相关代码和 [现有工作流](.github/workflows/verify.yml)。检查当前 `main`、开放 PR/Issue、相关分支及对应 head 的 CI；先处理有关的在途工作，不从旧版本号或旧运行报告选择新任务。
- 远端记录不能证明其他设备没有未推送改动或正在运行的任务；无法核实时标明未知，只暂停可能重叠的写入。同一工作副本只由一个写入者修改，独立任务使用独立分支。

## 换机与回写

- 不自动 stash、reset --hard、clean 或 force-push 来消除差异。提交和推送前重新核对远端，只带入本任务改动，不覆盖别的设备或他人的工作。
- 在重要决定、阶段完成、阻塞或换机时，按授权更新已有 PR/Issue 中的必要停点：决定与理由、实际验证范围、未决事项、下一步及仍未同步或仍在运行的工作。不复制 Git/CI 可直接查询的状态，不把本地未推送内容说成已同步。
- 本仓库公开。接手说明只写可公开的项目信息，不写私人资料、设备绝对路径、真实用户答案或凭据；不向 Obsidian 复制项目进度。

## 产品与验证

- 保留现有轻量静态站点、20 题、四条娱乐维度、16 型和一次作答一个结果；未经相应要求不改计分及类型映射。保留旧分享链接、答案不出浏览器、临时存储期限和娱乐/专业边界，不新增统计或答案上传。
- 纯文档改动检查 `git diff --check`、引用路径和实际差异。代码或素材改动按 README 与现有工作流运行 `node --test tests/*.test.cjs`、`python scripts/prepare-assets.py` 及 Chromium/WebKit 对应回归，不删断言、加重试或改超时来掩盖失败。
- 素材保持来源、署名和清单校验；测试输出沿用忽略目录，不顺带更新正式预览图。模拟系统分享、桌面 WebKit 与真实 iPhone/Safari 验收须区分，未测不能写成通过。

## 发布

- `main` 根目录是现有 GitHub Pages 发布源，更新 `main` 会触发现有发布；合并前必须核对当前有效发布授权，不能把文档改动或绿色 CI 当成授权。未经本次允许，不合并、不部署，也不改变工作流。
- 获准发布后，分别核对对应提交的检查、Pages 部署及 `scripts/check-live.py`/现有线上检查；PR 通过不等于网站已更新。失败或仍在运行时报告实际状态，不重复启动无变化的任务。
